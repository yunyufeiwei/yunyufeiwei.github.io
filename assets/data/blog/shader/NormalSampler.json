{
  "id": "untitled",
  "title": "纹理采样笔记：切线空间法线贴图采样",
  "date": "2026-05-28",
  "summary": "对法线贴图采样后，为什么有些shader制作工具中，需要再添加Unpack节点来修正输出效果进行粗略说明。",
  "tags": [
    "Shader",
    "纹理采样"
  ],
  "content": [
    "# 概述：",
    "",
    "在shader代码、ShaderGraph、ASE等多个shader编写方式中，各自都有自己的一些特殊情况，因此这里对法线贴图采样后，为什么有些shader制作工具中，需要再添加Unpack节点来修正输出效果进行粗略说明。",
    "",
    "",
    "",
    "",
    "",
    "# 内容:",
    "",
    "在美术基于PBR制作资产中，最后会产出3张贴图，颜色贴图，混合贴图（MRA），法线贴图，在shader代码中，通常会先先纹理采样出来。",
    "",
    "",
    "",
    "",
    "",
    "```hlsl",
    "half4 baseMap = SAMPLE_TEXTURE2D(_BaseMap , sampler_BaseMap , i.uv);",
    "half4 maskMap = SAMPLE_TEXTURE2D(_MaskMap , sampler_MaskMap , i.uv);",
    "half4 normalMap = SAMPLE_TEXTURE2D(_NormalMap , sampler_NormalMap , i.uv);",
    "half3 normalTS = UnpackNormalScale(normalMap , _NormalScale);       //使用采样之后的法线贴图数据，解包成切线空间下的法线信息",
    "```",
    "",
    "其中法线贴图通常制作的是切线空间的，由于渲染的计算需要再同一空间下才能正确计算，因此还需要将法线贴图转换到世界空间，保证后面的计算是在同一空间下进行，而把切线空间下的法线转换到世界空间，需要使用TBN矩阵进行变换。 在顶点阶段传入模型的法线和切线数据，并在片段阶段输出世界空间下的对应数据：",
    "",
    "",
    "```hlsl",
    "struct Attributes",
    "{",
    "    float3 normalOS     : NORMAL;",
    "    float4 tangentOS    : TANGENT;",
    "};",
    "",
    "struct Varyings",
    "{",
    "    float3 normalWS     : TEXCOORD2;",
    "    float3 tangentWS    : TEXCOORD3;",
    "    float3 bitangentWS  : TEXCOORD4;",
    "};",
    "```",
    "",
    "",
    "",
    "",
    "然后在顶点着色器阶段计算世界空间下的切线、法线、副切线，这里直接使用引擎封装好的函数进行传递。",
    "",
    "",
    "",
    "",
    "",
    "```hlsl",
    "Varyings vert (Attributes v)",
    "{",
    "    VertexNormalInputs normalInput = GetVertexNormalInputs(v.normalOS , v.tangentOS);",
    "    o.normalWS = normalInput.normalWS;",
    "    o.tangentWS = normalInput.tangentWS;",
    "    o.bitangentWS = normalInput.bitangentWS;",
    "}",
    "```",
    "",
    "这样就可以使用计算出来的信息，在片段输出中构造TBN矩阵，从而将切线空间的法线转换到世界空间。",
    "",
    "",
    "```hlsl",
    "half4 frag (Varyings i) : SV_Target",
    "{",
    "    half4 FinalColor;",
    "",
    "    Light light = GetMainLight();",
    "    half3 lightDir = light.direction;",
    "    half3 lightColor = light.color * light.distanceAttenuation;",
    "    ",
    "    half4 baseMap = SAMPLE_TEXTURE2D(_BaseMap , sampler_BaseMap , i.uv);",
    "    half4 maskMap = SAMPLE_TEXTURE2D(_MaskMap , sampler_MaskMap , i.uv);",
    "    half4 normalMap = SAMPLE_TEXTURE2D(_NormalMap , sampler_NormalMap , i.uv);",
    "    half3 normalTS = UnpackNormalScale(normalMap , _NormalScale);       //使用采样之后的法线贴图数据，解包成切线空间下的法线信息",
    "",
    "    //构建TBN矩阵",
    "    float3x3 TBN = float3x3(i.tangentWS,i.bitangentWS,i.normalWS);",
    "",
    "    //向量计算",
    "    float3 worldNormalDir = TransformTangentToWorld(normalTS , TBN);",
    "",
    "    //最终颜色输出",
    "    FinalColor = half4(worldNormalDir , 1.0);",
    "",
    "    return FinalColor;",
    "}",
    "```",
    "",
    "![切线空间下的法线采样](assets/ImageWork/Blog/Shader/切线空间下的法线采样.png)",
    "![世界空间下的法线采样](assets/ImageWork/Blog/Shader/世界空间下的法线采样.png)",
    "",
    "",
    "",
    "",
    "",
    "# 总结：",
    "",
    "在ASE中，需要注意采样法线贴图后，需要添加一个unpack节点，或者在采样法线贴图本身上勾选Unpack属性。",
    "",
    "在ShaderGraphs中，需要注意修改法线贴图采样时的模型，不要使用Default模式，应该正确选择Normal模式。"
  ]
}